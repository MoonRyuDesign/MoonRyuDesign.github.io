import React from 'react';
import MainContent from '../components/main/MainContent';

const Main = () => {
  return (
    <div style={{ height: '100%' }}>
      <MainContent />
    </div>
  );
};

export default Main;
